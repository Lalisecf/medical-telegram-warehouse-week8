version: 2

sources:
  - name: raw
    schema: raw
    description: "Raw data loaded into PostgreSQL"

    tables:

      - name: telegram_messages
        description: "Raw Telegram messages"

      - name: image_detections
        description: "YOLO object detection results loaded from detections.csv"